export interface Transaction {
  id: number;
  sinal: string;
  transaction: string;
  product: string;
  price: string;
}

export interface EXTTable {
  sinal: string;
  product: string;
  price: string;
}